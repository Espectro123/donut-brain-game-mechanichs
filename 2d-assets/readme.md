# 2D assets used

To add a new assets, just create a folder for it with a descriptive name.
We should reuse the assets always to avoid filling this folder with trash.

# Added Assets from [Kenny](https://kenney.nl/)

We have to compress/uncompress assets in a single .zip called assets.zip for git.
We will need to create a script to this. (Lazy)